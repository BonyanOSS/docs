<Note>
  Bonyan API normalizes provider responses into stable client-facing shapes.
  Most content modules use ordered fallback chains; modules without a second
  trusted source document that limit in their overview. See
  [Concepts -> Fallback](/concepts/fallback).
</Note>
